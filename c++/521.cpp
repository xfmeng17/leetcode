class Solution {
public:
    int findLUSlength(string a, string b) {
        
    }

    // ** straight forward
    int func1(string a, string b) {
    	if (b.find(a)) {
    		return a.length();
    	}
    	
    }
};
